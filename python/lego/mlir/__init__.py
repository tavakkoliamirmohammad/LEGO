# Marker file: makes lego.mlir a regular sub-package so that MLIR's
# relocated Python bindings (ir.py, dialects/, _mlir_libs/, etc.) are
# importable as lego.mlir.* after the build assembles them here.
